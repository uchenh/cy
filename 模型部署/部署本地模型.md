#### **安装Conda**

```bash
#取消代理
unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY
#查看代理
env | grep -i proxy  
#代理
export http_proxy="http://192.168.12.231:7897"
export https_proxy="http://192.168.12.231:7897"

#安装conda
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh -b -u -p $HOME/miniconda3
/root/miniconda3/bin/conda init bash
source ~/.bashrc
#接受Tos
conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main
conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r
```

#### **创建Conda环境**

```bash
##创建conda环境
conda create -n Qwen python=3.11 -y   # 若 vLLM 0.19+ 要求 3.12，再改成 3.12
conda activate Qwen

# 用 uv 让 vLLM 自动选匹配的 PyTorch + CUDA（推荐）
pip install uv
python3 -m pip install uv -i https://pypi.tuna.tsinghua.edu.cn/simple/
uv pip install -U "vllm>=0.19.0" --torch-backend=auto
uv pip install -U "vllm>=0.19.0" --torch-backend=auto -i https://mirrors.aliyun.com/pypi/simple
uv pip install -U "vllm>=0.19.0" --torch-backend=auto -i https://pypi.tuna.tsinghua.edu.cn/simple
#验证
python -c "import torch; print('CUDA:', torch.cuda.is_available()); print('GPUs:', torch.cuda.device_count()); print(torch.cuda.get_device_name(0))"
#openweb-ui
pip install open-webui
pip install open-webui -i https://pypi.tuna.tsinghua.edu.cn/simple
#启动openwebui
open-webui serve
#访问
localhost:8080
192.168.18.133:8080

pip install sentence-transformers onnx onnxruntime openvino -i https://mirrors.aliyun.com/pypi/simple
pip install "sentence-transformers<4.0" -i https://mirrors.aliyun.com/pypi/simple

open-webui serve --host 0.0.0.0 --port 8080
ufw allow 3000/tcp
http://192.168.18.133:3000/
```

#### **下载模型**

```bash

#下载modelscope
pip install modelscope
pip install modelscope -i https://pypi.tuna.tsinghua.edu.cn/simple
#下载模型到指定目录 
#Qwen3.5-35B-A3B
modelscope download --model Qwen/Qwen3.5-35B-A3B  --local_dir /model/Qwen
#cyankiwi/GLM-5.1-AWQ-4bit
modelscope download --model cyankiwi/GLM-5.1-AWQ-4bit  --local_dir /model/Glm
#Eco-Tech/Kimi-K2.6-w4a8 需要320g显存
modelscope download --model Eco-Tech/Kimi-K2.6-w4a8      --local_dir /model/Kimi
#Llama-4-Scout-17B-16E-Instruct  
modelscope download --model LLM-Research/Llama-4-Scout-17B-16E-Instruct  --local_dir /model/Llama
#gemma-4-31B
modelscope download --model google/gemma-4-31B-it  --local_dir /model/Google
#NVIDIA-Nemotron-3-Nano-30B-A3B-BF16
modelscope download --model nv-community/NVIDIA-Nemotron-3-Nano-30B-A3B-BF16  --local_dir /model/Nv
#gpt-oss-120b   
modelscope download --model openai-mirror/gpt-oss-120b   --local_dir /model/Gpt
#Kimi2.6-JANGQ
modelscope download --model JANGQ-AI/Kimi-K2.6-Med-JANGTQ   --local_dir /model/Kimi2.6-Med-JANGTQ
#gemma-4-31B-it
modelscope download --model google/gemma-4-31B-it  --local_dir /model/gemma-4-31B-it
#GLM-5.1-367B-A14B-REAP
modelscope download --model 0xSero/GLM-5.1-367B-A14B-REAP   --local_dir /model/Glm
modelscope download --model deepseek-ai/DeepSeek-V4-Flash   --local_dir /model/DeepSeek
modelscope download --model Qwen/Qwen3.5-35B-A3B  --local_dir /model/Qwen
modelscope download --model deepseek-ai/DeepSeek-R1-Distill-Qwen-14B  --local_dir /model/Deepseek

modelscope download --model Qwen/Qwen3.5-397B-A17B  --local_dir /mnt/nvme0n1/model/Qwen3.5
modelscope download --model moonshotai/Kimi-K2.6  --local_dir /mnt/nvme0n1/model/Kimi2.6
modelscope download --model deepseek-ai/DeepSeek-V4-Flash-Base  --local_dir /mnt/nvme0n1/model/Deepseekv4flashbase
modelscope download --model deepseek-ai/DeepSeek-V4-Flash  --local_dir /mnt/nvme0n1/model/Deepseekv4flash
modelscope download --model deepseek-ai/DeepSeek-V4-Pro  --local_dir /mnt/nvme0n1/model/Deepseekv4pro
modelscope download --model Qwen/Qwen3.5-122B-A10B-FP8  --local_dir /models/Qwen3.5-122B-A10B-FP8
modelscope download --model Qwen/Qwen3.6-35B-A3B  --local_dir /models/Qwen3.6-35B-A3B

modelscope download --model deepseek-ai/DeepSeek-V4-Flash  --local_dir /models/deepseek-v4-flash
modelscope download --model deepseek-ai/DeepSeek-V4-Flash-0731  --local_dir /models/DeepSeek-V4-Flash-0731

modelscope download --model Qwen/Qwen3.8-27B  --local_dir /models/Qwen3.8-27B

modelscope download --model ZhipuAI/GLM-5.3-Flash  --local_dir /models/GLM-5.3-Flash

modelscope download --model deepseek-ai/DeepSeek-V4-Flash-Vision-Exp --local_dir /models/DeepSeek-V4-Flash-Vision-Exp

modelscope download --model Qwen/Qwen3.8-Flash-Next --local_dir /models/Qwen3.8-Flash-Next

#后台下载
su - root -c "nohup bash -c '
source /root/miniconda3/etc/profile.d/conda.sh
conda activate Deepseek
modelscope download --model deepseek-ai/DeepSeek-V4-Pro --local_dir /mnt/nvme0n1/model/Deepseekv4pro
' > /root/deepseek_dl.log 2>&1 &"

#查看下载日志
tail -f /root/deepseek_dl.log
#查看后台进程
ps aux | grep modelscope

#数据集
modelscope download --dataset swift/sharegpt --local_dir /models/sharegpt
```

